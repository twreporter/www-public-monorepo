name: Dev Version Bump

on:
  push:
    branches:
      - dev

jobs:
  version-bump:
    if: github.repository == 'twreporter/www-public-monorepo'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.17.1'
      
      - name: Enable Corepack
        run: corepack enable
      
      - name: Install dependencies
        run: yarn install --immutable
      
      - name: Configure Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
      
      - name: Commit any uncommitted changes
        run: |
          # Check if there are any uncommitted changes
          if ! git diff --quiet || ! git diff --cached --quiet; then
            git add .
            git commit -m "chore: commit changes before version bump" || true
          fi
      
      - name: Bump version for all packages
        run: |
          yarn lerna version \
            --conventional-commits \
            --preid beta \
            --conventional-prerelease \
            --include-merged-tags \
            --message "chore(release): publish prerelease" \
            --yes

      - name: Authenticate to npm
        run: echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish tailwind-config if version bumped
        id: publish_tailwind_config
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q "packages/tailwind-config/package.json"; then
            cd packages/tailwind-config
            npm publish --access public
            cd ../..
            echo "published=true" >> $GITHUB_OUTPUT
          else
            echo "published=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish react-typescript-components if version bumped
        id: publish_react_typescript_components
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q "packages/react-typescript-components/package.json"; then
            cd packages/react-typescript-components
            npm publish --access public
            cd ../..
            echo "published=true" >> $GITHUB_OUTPUT
          else
            echo "published=false" >> $GITHUB_OUTPUT
          fi

      - name: Update yarn.lock and commit specific files
        run: |
          yarn install --immutable-cache
          git add yarn.lock
          git add package.json
          git add packages/*/package.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update yarn.lock and package.json files after version bump"
          fi

      - name: Push changes back to dev
        run: |
          git push origin dev --follow-tags
